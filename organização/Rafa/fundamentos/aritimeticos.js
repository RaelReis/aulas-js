const [a, b, c, d, e, f, g, h] = [1, 2, 3, 4, 5, 6, 7, 8]

const soma = a + b + c + d + e + f + g + h
const subtracao = a - b
const multi = a * b
const divi = a / b
const modulo = a % b

console.log(soma, subtracao, multi, divi, modulo)